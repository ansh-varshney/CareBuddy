export const environment = {
  production: false,
  apiUrl: '',        // Empty = use Angular dev server proxy (proxy.conf.json)
  wsUrl: 'ws://localhost:8000',  // WebSocket direct (proxy handles ws too)
};

